setup:
    config:
        MIX_ENV: prod
        SECRET_KEY_BASE: $SECRET_KEY_BASE
        DATABASE_URL: $DATABASE_URL 
build:
    docker:
        web: ./Dockerfile
    config:
        MIX_ENV: prod
        SECRET_KEY_BASE: $SECRET_KEY_BASE
        DATABASE_URL: $DATABASE_URL
run:
    web: bin/banking_api start